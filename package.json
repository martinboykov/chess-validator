{
  "name": "fork_point_task_chess",
  "version": "1.0.0",
  "description": "chess moves validator",
  "main": "index.js",
  "scripts": {
    "dev": "cross-env NODE_ENV=development DEBUG=*,-nodemon*,-snapdragon* nodemon index.js",
    "start": "cross-env NODE_ENV=production node index.js",
    "test": "jest --colors --runInBand --coverage test",
    "jest:watch": "jest --runInBand --watchAll",
    "jest": "npm run jest:watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "chalk": "^2.4.2",
    "cross-env": "^6.0.0",
    "debug": "^4.1.1",
    "jest": "^22.4.4",
    "nodemon": "^1.19.2"
  }
}
